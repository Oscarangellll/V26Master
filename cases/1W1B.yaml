# vessel_types: optional, default is all vessel types
# max_multiday_vessels: required
# bases: required, list of bases 
# one_base: required
# wind_farms: required, list of wind farms

max_multiday_vessels: 2

bases: ["3"]

one_base: True

wind_farms: ["C"]
